# Awesome clipboard replace man(ACRM)

# How to use
- `npm install` or `yarn install`
- Edit replacements inside `src/index.ts`
- `npm start` or `yarn start`
- Enjoy!!

# Milestone
- Make it more awesome
- CLI Tool
- More replacement options